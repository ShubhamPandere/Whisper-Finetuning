# PyTorch stack for Windows + CUDA 12.1
torch==2.2.2+cu121
torchvision==0.17.2+cu121
torchaudio==2.2.2+cu121 --index-url https://download.pytorch.org/whl/cu121

# Hugging Face stack pinned to a mutually compatible set
transformers==4.40.2
tokenizers==0.19.1
huggingface-hub==0.23.0
datasets==2.19.1
accelerate==0.27.2
peft==0.10.0
evaluate==0.4.2

# Audio / utility deps
librosa
soundfile
jiwer
matplotlib
pandas
tqdm
wandb

# UI and Post-processing
gradio
indic-nlp-library
morfessor
onnxruntime
noisereduce

# QLoRA quantization
# Optional on Linux/WSL2. Native Windows usually falls back to standard LoRA.
# Install bitsandbytes manually only if your environment supports it.
